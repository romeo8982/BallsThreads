#include <iostream>
#include "Ball.h"
using namespace std;

int main()
{
    Ball ball(1.5,0.0,0.0);
   
    ball.setVolacity(3.0);
    ball.setXPosition(3.0);
    ball.setYPosition(3.0);
  

    return 0;
}
